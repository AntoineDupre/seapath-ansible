---
- name: Converge (sysctl only)
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Run only sysctl.yml from role centos
      ansible.builtin.include_role:
        name: centos
        tasks_from: sysctl.yml
